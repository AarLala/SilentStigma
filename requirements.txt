# Web Framework (Production Dashboard)
flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
gunicorn>=21.2.0

# Data Processing (for reading CSV files)
pandas>=2.0.0

# Database
supabase>=2.0.0

# Caching (optional - for better performance with Gunicorn workers)
# redis>=5.0.0  # Uncomment if using Redis for rate limiting

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
